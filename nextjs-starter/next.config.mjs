/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['emmiaqzgxczelhtfzvmr.supabase.co'],
    },
    reactStrictMode: true,
  };
  
  export default nextConfig;
  